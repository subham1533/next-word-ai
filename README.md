# next-word-ai
Full Stack AI project that predicts the next word in a sentence using LSTM (Deep Learning), FastAPI backend and a simple web frontend. This is a prototype version which will be scaled to a larger dataset and deployed for public use.
